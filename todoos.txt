later make that line 77 in tsconfig to true and 78 to true as well, then further resolve the errors



modify contexts and then strict set to false and test app, then header siderbars, then each hook and its test

working TS version of whole app


opitimize the interfaces using extend, use generics somewhere


tell return type jsx.element, everything must have return type
enable linting for more robust handling
when to use unknown never any




Will think how just take verification code on that page and not the email, have to keep context, but will do it later currently more focus on backend